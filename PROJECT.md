# АРТДОМ — премиальная недвижимость

Лендинг + посадка на WordPress. Макет нарисован Павлоном в Photoshop, исходник `004.psd` (1920×8040, контент до 6736), превью `artdom.jpg`.

Все числа ниже **прочитаны из PSD**, а не сняты пипеткой с JPG. Спорные — перепроверены замером по превью; расхождения описаны в разделе «Ловушки».

---

## 1. Сетка и секции

Холст 1920. Контент от **x=44** до **x=1877** → **контейнер 1832px, поля 44px**.
Макет краевой (edge-to-edge), не центрированный «в 1200».

| # | Секция | Y от–до | Высота | Фон |
|---|---|---|---|---|
| 1 | Hero | 0–1040 | 1040 | фото + оверлей `#2b2e33` 35% |
| 2 | Услуги | 1040–1851 | 811 | `#edeff4` |
| 3 | Избранные объекты | 1851–2905 | 1054 | `#ffffff` |
| 4 | Экспертные решения (о компании) | 2905–3884 | 979 | `#edeff4` |
| 5 | Надёжность и гарантии | 3884–4847 | 963 | фото + оверлей `#2b2e33` 35% |
| 6 | Цифры | 4847–5319 | 472 | `#ffffff` |
| 7 | Честная оценка (отзывы) | 5319–5894 | 575 | `#edeff4` |
| 8 | Футер | 5894–6736 | 842 | `#000000` |

Ритм фонов: тёмный → серый → белый → серый → тёмный → белый → серый → чёрный.

---

## 2. Палитра

| Токен | Значение | Где |
|---|---|---|
| `--c-ink` | `#000003` | заголовки, цены, характеристики |
| `--c-ink-soft` | `#202023` | пункты аккордеона |
| `--c-muted` | `#6a6d81` | корпусный текст, чипы, подписи |
| `--c-accent` | `#4d6dd9` | кнопки, ссылки, цифры, звёзды |
| `--c-surface` | `#edeff4` | фон светло-серых секций |
| `--c-chip` | `#edeef4` | плашка чипа «2026» |
| `--c-white` | `#ffffff` | фон белых секций, текст на тёмном |
| `--c-black` | `#000000` | футер |
| `--c-rule` | `#d9dae0` | разделители (при 70% прозрачности → `#e6e5ea`) |
| `--c-rule-track` | `#dfdfe4` | трек полосы прокрутки слайдера |
| `--c-scrim` | `#2b2e33` @ 35% | затемнение фото |

Аномалия: ссылка **WhatsApp** в футере покрашена `#7595ff`, соседние Telegram и VK белые — см. «Вопросы к макету».

---

## 3. Типографика

Гарнитура — **Inter**, в двух оптических размерах: `Inter 18pt` (почти везде) и `Inter 28pt` (навигация, футер).
В вебе это одна переменная гарнитура с осью `opsz`; воспроизводится точно через `font-variation-settings: 'opsz' 18` / `'opsz' 28`.

Логотип «Артдом» — **BOWLER Regular**, единственное вхождение. Выносим в SVG, шрифт не подключаем.

| Роль | Кегль / интерл | Начертание | Цвет |
|---|---|---|---|
| H1 hero | 70 / 72 | SemiBold | `#ffffff` |
| H2 крупный (по центру) | 70 / 72 | SemiBold | `#000003` / `#ffffff` на фото |
| H2 секции | 40 / 28¹ | Regular | `#000003` |
| Заголовок карточки, имя в отзыве, цена | 28 / 28 | Regular | `#000003` |
| Лид hero | 21 / 28 | Regular | `#ffffff` |
| Цифры | 90 / 72 | Medium | `#4d6dd9` |
| Пункт аккордеона | 18 / 29 | Medium | `#202023` |
| Текст кнопки | 18 / 22 | Regular | `#ffffff` |
| Подпись под цифрой | 18 / 26 | Regular | `#6a6d81` |
| Корпусный текст | 16 / 24 | Regular | `#6a6d81` |
| Навигация | 16 | Regular (28pt) | `#ffffff` / `#ffffff` в футере |
| Чип | 14 / 28 | Regular | `#6a6d81` |
| Футер: телефон / e-mail / адрес | 40 / 24 / 18 | Regular (28pt) | `#ffffff` |
| Футер: меню | 21 | Regular (28pt) | `#ffffff` |

¹ Интерлиньяж 28 при кегле 40 — так Photoshop хранит однострочный заголовок. В вёрстке ставим ≈1.1, иначе строки слипнутся при переносе.

**Трекинг везде 0.** Разрядку не добавляем (правило студии).

---

## 4. Компоненты

| Компонент | Размер | Детали |
|---|---|---|
| Кнопка большая | 291×90 | синяя, текст 18/22 белый, стрелка 23×6 справа |
| Кнопка малая | 213×43 | текст 16 `#4d6dd9`, стрелка |
| Чип категории | h 33 | скругление, текст 14 `#6a6d81` |
| Чип года | 71×36 | фон `#edeef4` |
| Аватар отзыва | 89×89 | круг, инициалы 28 |
| Фото карточки объекта | 580×395 | |
| Иконка «+» аккордеона | 38×38 | круг `#4d6dd9` |
| Портрет основателя | 181×181 | круг |
| Полоса слайдера | 626 трек / 332 бегунок | `#dfdfe4` / `#4d6dd9`, h 4 |
| Разделитель | h 1 | `#d9dae0` при 70% |

Слайдеров два: **Избранные объекты** и **Отзывы** — оба по 3 карточки в кадре, полоса прокрутки внизу по центру. Бегунок занимает 53% трека → в ленте примерно 6 карточек.

---

## 5. Ловушки этого PSD

1. **У 15 текстовых слоёв висит «наложение цвета» `#9c9eab`, и оно ВЫКЛЮЧЕНО.** В файле стоит `effects.disabled: true`, при этом сам эффект внутри помечен `enabled: true`. Кто читает только эффект — красит корпусный текст в `#9c9eab` вместо истинного `#6a6d81` и заваливает контраст ещё сильнее. Поймано замером гистограммы по JPG: плато на `#696c83`, следов `#9c9eab` нет.
2. **Цвета с JPG врут в обе стороны.** Пипетка по превью давала синий `#376edd` вместо истинного `#4d6dd9` — это звон JPEG на резких краях, он даёт и пере-, и недосвет. Для этого макета **источник истины — PSD**, превью только для проверки «включён ли эффект».
3. Холст 8040, контент кончается на 6736 — снизу 1304px пустоты. Ничего не потеряно.
4. Слой `Слой 15` в секции объектов скрыт — в макете не участвует.

---

## 6. Вопросы к макету

Найдено при разборе, ждёт решения Павлона:

1. «Покупка и **покупка**» — первый пункт аккордеона. Похоже на «покупка и продажа».
2. Описание под «Услуги» и под «Избранные объекты» — **один и тот же текст** про сопровождение сделки. Во второй секции не по смыслу.
3. Карточки 1 и 3 идентичны: 180 м², 4 спальни, 12 этаж, 89 млн ₽, обе «Новостройка». Различаются только названием.
4. Во всех трёх карточках адрес «Москва, Ленинский проспект» и текст-заглушка.
5. Заголовок первой карточки синий `#4d6dd9`, у остальных чёрный — похоже, показано состояние наведения.
6. WhatsApp в футере голубой, Telegram и VK белые — то же самое.
7. Цифра «<1» с подписью «Среднее время ответа, меньше часа» — смысл продублирован.
8. Блок «38 / Млн ₽ / Средний бюджет сделки»: единица измерения ушла в подпись, у трёх соседей подписи чисто описательные — ритм из четырёх ломается.
9. Мобильной версии в PSD нет.
10. Поведение шапки при прокрутке не задано (на фото она белая, дальше идут светлые секции).

---

## 7. Контраст (WCAG AA)

| Пара | Замер | Норма | Итог |
|---|---|---|---|
| `#6a6d81` на `#edeff4` — корпусный 16px | **4.43:1** | 4.5 | не проходит на волосок |
| `#6a6d81` на `#edeef4` — чип 14px | **4.41:1** | 4.5 | не проходит |
| `#4d6dd9` на `#edeff4` — ссылка 16px | **4.05:1** | 4.5 | не проходит |
| `#6a6d81` на `#ffffff` | 5.10:1 | 4.5 | ок |
| `#ffffff` на `#4d6dd9` — кнопка | 4.66:1 | 4.5 | ок |
| `#4d6dd9` на `#ffffff` — цифры 90px | 4.66:1 | 3.0 | ок |
| `#000003` на `#edeff4` — заголовки | 18.23:1 | 4.5 | ок |
| `#ffffff` на `#000000` — футер | 21.00:1 | 4.5 | ок |

Промахи копеечные и лечатся двумя пунктами яркости: `#6a6d81` → **`#686b7f`** даёт 4.57:1. Синюю ссылку на сером фоне — либо чуть темнее, либо оставить только на белом.

---

## 8. База WordPress

Тема студии **`oxboxwise`** (Underscores + ACF + Swiper + jQuery validate/mask), архив `oxboxwise.zip`, 89 файлов.

**Берём:** скелет `functions.php` + `inc/`, соглашение `template-parts/`, страница настроек ACF, `ajax-request.php` под формы, пагинацию, регистрацию меню (`menu_main` + `menu_footer` — ровно наш случай), Swiper, валидацию и маску телефона.

**Не берём (остатки прошлого проекта):**

| Что | Размер | Почему |
|---|---|---|
| `css/critical.css` | 22 КБ | шрифт Ubuntu, цвета `#42413d`/`#f8f8f8`, шапка 133px, контейнер 1550 — чужая система |
| `css/libs.min.css` | 33 КБ | то же |
| `style-rtl.css`, `woocommerce.css` | 26 КБ | RTL и магазина нет |
| `js/utils.js` | **252 КБ** | даже не подключён |
| `img/flags.png` + `@2x` | 245 КБ | под intl-tel-input, он закомментирован |
| `fonts/montserrat-*.eot` | 66 КБ | EOT — формат для IE8, одно начертание |

**Дефекты базы, которые надо починить в любом случае:**

1. `inc/enqueue-script-style.php` подключает `js/libs.min.js` — **этого файла в архиве нет**, 404 на каждой загрузке.
2. jQuery тянется с `ajax.googleapis.com`. В РФ это лишний риск и лишнее рукопожатие; WP несёт jQuery локально.
3. Короткие теги `<?` в `header.php`, `footer.php`, `content-header.php`. При выключенном `short_open_tag` (по умолчанию с PHP 5.4) вывалятся сырым текстом.
4. `<meta name="viewport" ... maximum-scale=1>` — запрещает щипковое увеличение. Провал доступности и замечание Lighthouse.
5. `css/custom.css` несёт зелёный `#7a9e53` из прошлого проекта.
6. `templates/template-mainpage.php` содержит текст «DevOps Services».
7. `template-parts/content-header.php` — меню зашито в разметку («Доставка и оплата»), переключатель языка, поиск, жёлтый бургер `#f3b946`.

---

## 9. План

1. **Статическая вёрстка** — HTML/CSS/JS, открывается с диска двойным кликом. Круги правок дешёвые: Павлон смотрит, я правлю, никакого сервера.
2. **Замеры → токены**, числа из таблиц выше.
3. **Проверки** — геометрия на трёх ширинах, зоны нажатия, контраст, состояния наведения.
4. **Мобильная версия** — сначала показываю раскладку, потом верстаю.
5. **Посадка на WP** — темплейты, ACF-поля кодом, CPT «Объекты» и «Отзывы», две формы через `ajax-request.php`.
6. **Сборка zip** темы.
